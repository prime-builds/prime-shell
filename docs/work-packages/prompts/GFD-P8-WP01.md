# GFD-P8-WP01 — Template Extraction and Second Branded Application

## 1. Package identity and prompt status

**Package ID:** `GFD-P8-WP01`
**Phase:** `Phase 8`
**Title:** `Template Extraction and Second Branded Application`
**Task ID:** `GFD-P8-WP01`
**Prompt ID:** `PRIME-SHELL-GFD-P8-WP01-PROMPT`
**Prompt version:** `R1`
**Prompt lifecycle state:** `Provisional`
**Implementation status at authoring:** `Not started`
**Recommended future implementation model:** `GPT-5.6 Sol`
**Recommended future implementation reasoning/intelligence:** `High`
**Authorization boundary:** Exactly one package, `GFD-P8-WP01`
**Execution status:** `Execution is not authorized`

High is the roadmap minimum because this package must distinguish proven
two-application invariants from product-specific behavior, preserve independent
application identity and native state, extract only evidence-backed template
surfaces, and prove deterministic clean generation and packaging for both
consumers. A future user-selected higher reasoning setting does not change this
roadmap minimum.

This provisional prompt is complete for review but non-executable. Chat Session
must first accept it as an Approved provisional prompt and later issue a
separately activated revision after every prerequisite is accepted and merged
and a real second-application consumer is fully specified. Prompt acceptance is
not activation, implementation authorization, generator execution, second-app
creation, publication authority, PR authority, or merge authority.

**Direct prerequisite:** Accepted and merged `GFD-P7-WP02`, including its
accepted application/updater trust separation, protected credential boundary,
signed and notarized target evidence, signed-update channel policy, recovery
evidence, implementation report, evidence index, manifest, snapshot, and exact
limitations.

**Activation prerequisite:** A real second branded application consumer with a
named owner, approved product and brand identity, bounded feature and domain
needs, exact target/package matrix, evidence owner, and explicit exclusions.

**Direct dependent:** `None`. This is the final package in the accepted
roadmap. Completion does not imply a successor package, public framework,
publication action, customer rollout, or continuing extraction program.

### Activation metadata

```text
Activation ID: Not activated — Chat Session must refresh and supply this exact value.
Activated by: Not activated — Chat Session must refresh and supply this exact value.
Activation UTC: Not activated — Chat Session must refresh and supply this exact value.
Authoritative main SHA: Not activated — Chat Session must refresh and supply this exact value.
Required fresh implementation branch: Not activated — Chat Session must verify absence and supply this exact value.
Accepted predecessors through GFD-P7-WP02: Not activated — Chat Session must supply exact merged commits, reports, artifacts, hashes, CI runs/jobs, native/manual evidence, measurements, deviations, fallbacks, limitations, exceptions, and blockers.
Accepted P5 two-consumer internal contract: Not activated — Chat Session must supply exact feature, route, navigation, command, shortcut, settings, registry, validation, ownership, source, and evidence facts.
Accepted P7 production and trust boundary: Not activated — Chat Session must supply exact capabilities, CSP, package identities, signing identities, updater verifier and channel policy, protected-environment rules, and publication prohibitions.
First application identity: Not activated — Chat Session must supply exact product name, internal app ID, bundle identifier, executable, package names, storage namespace, single-instance namespace, sidecar identity, updater identity, channel eligibility, and target matrix.
Second application owner and approved brief: Not activated — Chat Session must supply exact accountable owner, purpose, bounded domain, real user journey, feature needs, exclusions, and acceptance owner.
Second application brand identity: Not activated — Chat Session must supply exact product name, internal app ID, bundle identifier, executable, package names, publisher/vendor wording, icons/assets, theme accents, descriptions, and legal ownership.
Second application feature and operation set: Not activated — Chat Session must supply exact static features, routes, navigation, commands, shortcuts, settings, backend operations, native intents, diagnostics contribution, and deliberately absent capabilities.
Second application target and package matrix: Not activated — Chat Session must supply exact OS versions, architectures, webview engines, package formats, installation identity, runners, hardware/manual owners, and unavailable evidence.
Two-application extraction matrix: Not activated — Chat Session must supply every proposed shared surface, both real use sites, invariant or variant classification, retained owner, and non-extraction decision.
Template manifest schema and version: Not activated — Chat Session must supply exact bounded fields, validation, defaults, required values, normalization, reserved identifiers, unknown-field policy, and compatibility scope.
Template source and generated-output boundaries: Not activated — Chat Session must supply exact shared/template roots, app-owned roots, generated roots, protected roots, and ownership for every candidate path.
Composition and generation workflow: Not activated — Chat Session must supply exact generator or deterministic copy/composition command, versions, inputs, outputs, overwrite policy, collision policy, idempotence rule, and clean-target behavior.
Brand asset policy: Not activated — Chat Session must supply exact formats, dimensions, color and contrast rules, platform variants, licensing, provenance, validation, and generated-resource policy.
Per-application state isolation: Not activated — Chat Session must supply exact settings, cache, logs, diagnostics, native references, single-instance, temporary, update, and uninstall namespaces plus migration and collision rules.
Per-application trust isolation: Not activated — Chat Session must supply exact capabilities, permissions, CSP, package signing, updater verifier, channel, protected workflow, and credential-reference boundaries without secret values.
Shared sidecar and contract strategy: Not activated — Chat Session must supply exact shared protocol/schema source, app-specific operation allowlists, build IDs, packaged resources, Python module boundaries, and drift checks.
Fresh-copy and clean-target proof: Not activated — Chat Session must supply exact empty-directory generation, build, package, install, native journey, artifact inspection, cleanup, and reproducibility procedures for both applications.
Accepted target evidence matrix: Not activated — Chat Session must refresh exact automated, native, manual, inaccessible, blocked, and owner facts for each application and target.
Accepted targeted amendments: Not activated — Chat Session must refresh and supply the exact list or None.
Predecessor deviations incorporated: Not activated — Chat Session must refresh and supply the exact list or None.
Unresolved blockers and assumptions: Not activated — Chat Session must refresh and supply the exact list or None.
Authorization boundary: Not activated — a future activation may authorize only GFD-P8-WP01.
Authorization invalidates when: Not activated — a future activation must invalidate on any base-SHA, predecessor acceptance, second-application brief, identity, brand, feature/operation, target/package, extraction matrix, manifest, generator, path, state namespace, trust/channel, tool, evidence-owner, publication-state, repository-layout, or authorization-boundary change.
```

## 2. Repository and exact starting state

**Repository:** `prime-builds/prime-shell`
**Authoritative future base branch:** `main`
**Required future starting commit:** Not activated — Chat Session must refresh
and supply the exact accepted and merged `main` SHA after `GFD-P7-WP02`
acceptance and merge.
**Required future implementation branch:** Not activated — Chat Session must
verify absence and supply one exact fresh branch name.

Authoring-time documentation facts, for review only:

- accepted WP01 squash-merged `main`:
  `35d53bffec6e5b05aefdf8c7fed39a9bfdf288a2`;
- accepted Stage 1 foundation head:
  `cf2c2613d1facca1e61ba03eab88078a0fc1ffbf`;
- approved-provisional P7-WP01 prompt documentation head:
  `3567523b7afff460ffa82ff52fa1a13972459618`;
- approved-provisional P7-WP02 prompt documentation head:
  `41684c9e3c35feb2627a6408f37de34e6e800729`;
- P7-WP02 prompt parent:
  `3567523b7afff460ffa82ff52fa1a13972459618`;
- P7-WP02 prompt tree:
  `2e0cae4e0378a395a10289e5aac2d9d570f54472`;
- P7-WP02 prompt blob:
  `14a7d8bd163730c4bdd426dc7c3a58494f593d43`;
- P7-WP02 prompt SHA-256:
  `af44cc79be98770d2a88a3d4999790ccc407e045c87f448feefe4bb948ff447f`;
- accepted P7-WP02 authoring run:
  `20260728T194958Z`;
- accepted P7-WP02 authoring-report SHA-256:
  `c4ad1e2a51d305941d3067cbbc16a35625f29c0d123efd8667e133f528d4d745`;
- accepted P7-WP02 review-evidence-index SHA-256:
  `0b9b903e92dbe1c01c5e5742a992914586314d9397da10907831c494795b137b`;
- accepted P7-WP02 canonical manifest self-hash:
  `ff277e89f8814000a68b2f220cd85f351aa78a28a9ac9c5d474b7b860547a640`;
- prompt-pack documentation branch:
  `docs/gfd-work-package-prompt-pack-v1`;
- Phase 0B activation and implementation: `Implemented` and merged (closed at tag `v0.2.0-phase0b-closure`); Phase 1 through P7-WP02: `Not started`;
- `GFD-P8-WP01` implementation: `Not started`.

The authoring-time application remains the accepted WP01 Unicode echo spike.
It contains one React/Tauri application, one finite `spike.echo` operation,
one packaged Python sidecar, and no accepted Phase 2–7 implementation. It has
no application-template manifest, no deterministic app generator, no second
branded application, no multi-application workspace, and no evidence-backed
cross-application extraction surface. Future activation must discover actual
accepted merged paths and both real consumers. This prompt does not invent
future source paths, product semantics, brands, assets, packages, identities,
credentials, signed artifacts, channels, or support claims.

Required future activation and Work Session preflight:

1. Verify exact read/write access to `prime-builds/prime-shell`, default branch
   `main`, and the exact accepted merged 40-character starting SHA.
2. Verify WP01 and every package through P7-WP02 are accepted and merged at
   that SHA.
3. Reconcile every accepted report, evidence index, manifest, source snapshot,
   digest, workflow run/job/artifact, native/manual result, measurement,
   deviation, fallback, limitation, exception, amendment, and blocker.
4. Verify the real second application has an accountable owner, approved
   brief, non-placeholder brand, bounded feature/domain needs, exact target
   matrix, and evidence owners.
5. Inventory current workspace packages, application roots, feature registry,
   routes, navigation, commands, shortcuts, settings, schemas, generated
   bindings, Rust crates/modules, Python packages, sidecar resources, Tauri
   configuration, capabilities, CSP, package/signing/updater configuration,
   workflows, scripts, documentation, locks, and tests.
6. Inventory every identity and namespace used by the first application:
   product, bundle, executable, packages, settings, cache, diagnostics,
   single-instance, sidecar, updater, channels, storage, and uninstall.
7. Verify no template, generator, second app, cross-application public SDK,
   dynamic plugin system, consistency-review change, public publication, or
   customer rollout exists unexpectedly.
8. Verify the named fresh implementation branch is absent.
9. Use a clean fresh clone or worktree at the exact activated commit.
10. Replace broad path categories with one exact narrow allowlist and explicit
    protected-path inventory.
11. Resolve all four implementation deliverable names from one run ID and stop
    on any collision before writing.
12. Stop if state differs, the second consumer is not real and bounded, a
    shared surface lacks two consumers or a necessary invariant, unrelated
    work would be overwritten, or history rewriting would be required.

The accepted merged repository is authoritative over snapshots, documentation
branches, unmerged implementation branches, chat summaries, provisional
prompts, and superseded handoffs.

## 3. Mandatory authority and reading order

Read completely, in this order, before changing repository files:

1. `AGENTS.md`
2. `docs/authority/Core-Functionality-DeliveryRules.md`
3. `docs/authority/generic-fluent-desktop-project-handoff.md`
4. `docs/authority/generic-fluent-desktop-app-architecture-v0.2.md`
5. `docs/authority/architecture-review-consolidation-decision-log.md`
6. all accepted Markdown under `docs/phase-0a/`
7. `docs/work-packages/roadmap-index.md`
8. `docs/work-packages/shared-ground-rules.md`
9. `docs/work-packages/dependency-matrix.md`
10. accepted WP01 and P0B-WP02/P0B-WP03 source, prompts, activations,
    reviews, reports, artifacts, hashes, lifecycle, platform, package,
    process, measurement, deviation, fallback, and blocker evidence
11. accepted P1-WP01 through P3-WP02 prompts, activations, source, reports,
    manifests, snapshots, toolchains, schemas, native intent, task/runtime,
    capabilities, package, process, measurements, deviations, and blockers
12. accepted P4-WP01 and P5-WP01 prompts, activations, source, reports,
    manifests, snapshots, both real feature consumers, friction and extraction
    matrices, static contribution contracts, registries, validators,
    accessibility, package, measurement, deviation, and blocker evidence
13. accepted P6-WP01 and P6-WP02 prompts, activations, source, reports,
    manifests, snapshots, settings, migrations, single-instance, diagnostics,
    privacy, repair, recovery, package, measurement, deviation, and blockers
14. accepted P7-WP01 and P7-WP02 prompts, activations, source, reports,
    manifests, snapshots, production hardening, target artifacts, dependency
    closure, SBOM/licenses, signing/updater trust, protected workflows,
    channels, native update, recovery, measurements, limitations, and blockers
15. the approved second-application brief, brand and asset authority,
    product/feature/domain requirements, target/package matrix, evidence
    ownership, legal/provenance facts, and explicit exclusions named by
    activation
16. every separately accepted targeted architecture amendment named by
    activation
17. current workspace/package/application roots; feature, route, navigation,
    command, shortcut, settings, diagnostics, schema, generated binding,
    Rust, Python, Tauri, capability, CSP, sidecar, state, native intent,
    single-instance, signing, updater, channel, build, package, workflow,
    toolchain, manifest, lockfile, test, documentation, and support surfaces
18. the exact activated revision of this prompt

The first future implementation progress message must state:

> `AGENTS.md, repository authority, accepted Phase 0A, accepted and merged
> predecessors through GFD-P7-WP02, their accepted reports and artifacts, the
> approved real second-application brief and brand/feature/target evidence,
> the complete two-application source and ownership inventory, and the
> activated GFD-P8-WP01 prompt have been read and are active. Executing only
> GFD-P8-WP01 with GPT-5.6 Sol / High. Public SDK/plugin work, dynamic loading,
> public publication, customer rollout, product work outside the approved
> second consumer, PR creation, and merge remain unauthorized.`

If a required source is missing, unreadable, stale, placeholder-only, or
materially inconsistent, stop before writing. Do not substitute a broad
architecture review, a mock consumer, a renamed copy, a speculative framework,
or invented branding and product facts.

## 4. Dependencies, predecessor outputs, and prerequisite evidence

**Direct prerequisite:** Accepted and merged `GFD-P7-WP02`, including exact
application/updater trust separation, protected credential boundaries,
Windows and macOS trust evidence, truthful Linux boundaries, stable/beta
channel isolation, native update and recovery results, source, reports,
artifacts, hashes, measurements, limitations, and blockers.

**Required extraction prerequisite:** Accepted and merged `GFD-P5-WP01`,
including two real internal feature consumers and its proven minimal static
feature, route, navigation, command, shortcut, settings, availability,
validation, and Rust-authorization-independent contracts.

**Required real-consumer prerequisite:** One approved second branded
application that is materially distinct from the first application in brand
and bounded product/domain needs, has an accountable owner, and must actually
build and run. A fixture, renamed package, theme variant, screenshot, generated
stub, test-only consumer, sample, or hypothetical roadmap item is insufficient.

**Inherited prerequisites:** Accepted and merged WP01 and every package from
P0B-WP02 through P7-WP01, with exact accepted evidence and limitations.

**Direct dependent:** `None`.

P8-WP01 cannot activate while any predecessor exists only as a provisional
prompt, unmerged branch, unaccepted evidence set, stale target result, or
unresolved blocker that affects shared source, application identity,
packaging, state, capabilities, trust, or reproducibility.

Activation must supply exact:

- merged predecessor commits, trees, reports, evidence indexes, manifests,
  snapshots, byte lengths, and digests;
- accepted shell, theme, accessibility, feature-contribution, operation,
  task, settings, diagnostics, persistence, process, package, signing, updater,
  and recovery owners and invariants;
- first-application source, identity, namespace, feature, operation,
  capability, package, signing, updater, target, and evidence inventories;
- second-application owner, purpose, brand, IDs, assets, bounded domain,
  features, operations, settings, native intents, diagnostics, packages,
  signing/updater disposition, targets, and explicit exclusions;
- exact two-consumer use sites for each proposed shared surface;
- exact variant points that must remain per application;
- template manifest schema, normalization, validation, version policy, and
  compatibility boundary;
- deterministic composition/generation tool, versions, inputs, outputs,
  ownership, overwrite and collision policy;
- per-application settings, logs, diagnostics, cache, single-instance,
  sidecar, update, package, and uninstall isolation;
- exact build/package/install/native journeys and target-specific evidence
  owners for both applications;
- measurements, deviations, fallbacks, limitations, accepted exceptions,
  targeted amendments, and unresolved blockers.

No prerequisite or extraction decision may be accepted by inference.
Acceptance of this prompt does not activate implementation, authorize another
application not named by activation, expose a public API, begin publication,
or approve a consistency-review rewrite of earlier prompts.

## 5. Objective and measurable runnable outcome

Extract the smallest deterministic internal application template justified by
two real branded applications, use it to materialize both consumers without
identity or state collisions, and prove both applications build, package,
install, launch, and run their accepted bounded journeys from fresh copies.

The future package must prove this progression:

```text
accepted first application and full predecessor evidence
→ approved real second-application brief and independent brand/identity
→ complete two-application inventory before extraction
→ second application implemented through current accepted direct/static patterns
→ exact shared-versus-variant evidence matrix
→ smallest internal versioned template manifest and static composition boundary
→ deterministic bounded generator or copy/composition workflow
→ first and second applications migrated to the same proven shared template surfaces
→ independent app IDs, state namespaces, operations, capabilities, packages, and trust
→ fresh empty-directory materialization for each application
→ locked source build, native package, installed launch, accepted journey, and cleanup for both
→ drift, collision, secret, path, product-leakage, and forbidden-surface checks
→ review-ready internal template evidence with no public publication or rollout
```

The result must include two real, visibly distinct, locally runnable branded
applications. Each must have its own approved product identity, app identifier,
executable/package names, assets, state namespace, single-instance identity,
feature set, operation allowlist, capabilities, and target package identity.
Shared code must remain internal to this repository and must be justified by
both consumers or by one necessary invariant required to compose them safely.

The second application may implement only the bounded domain and feature needs
named by activation. It may reuse accepted shell and infrastructure, but it
must not become a vehicle for unrelated customer features, a broad product
roadmap, a public SDK, or public release.

A second directory containing renamed first-app bytes, a manifest with no real
consumer, a generator that produces stubs, types without execution, a browser
mock, a source build without native packaging, or one application standing in
for the other cannot satisfy the outcome.

## 6. Explicit in-scope work

The future activated Work Session may perform only the smallest coherent
change set needed for the following work.

### 6.1 Approved second-application definition

- Freeze the second application's accountable owner, purpose, bounded domain,
  accepted user journey, feature set, operation needs, settings, diagnostics,
  target packages, and explicit non-goals before writing.
- Freeze a non-placeholder brand identity: product name, internal app ID,
  bundle identifier, executable, package names, publisher/vendor wording,
  descriptions, icons, platform assets, and theme accents.
- Verify asset provenance, licensing, accessibility, platform requirements,
  safe formats, dimensions, and absence of secrets or user content.
- Require meaningful functional distinction from the first app while keeping
  scope small enough to prove template boundaries.
- Reject a second consumer manufactured only to justify a predesigned API.

### 6.2 Inventory before extraction

Inventory both applications' required:

- bootstrap, provider, window, theme, title-bar, portal, shell, responsive,
  accessibility, route, navigation, command, shortcut, and settings surfaces;
- feature definitions, static registry, availability, operation contracts,
  frontend adapters, state owners, native intents, Rust commands, sidecar
  behavior, diagnostics, and recovery;
- product/brand strings, assets, identifiers, paths, namespaces, package
  metadata, capabilities, CSP, signing, updater, channels, workflows, and
  evidence;
- shared build tools, manifests, locks, generated outputs, scripts, tests, and
  documentation.

For every candidate, classify it as:

- shared invariant used by both applications;
- shared implementation with explicit typed per-app configuration;
- app-owned variant;
- first-app-only behavior;
- second-app-only behavior;
- build/test-only surface;
- protected trust or credential reference;
- intentionally unextracted;
- blocked.

Record exact source paths and both real use sites. No extraction may precede
this inventory.

### 6.3 Consumer-before-template sequence

- Implement the second app first through current accepted static/internal
  patterns far enough to prove its real identity, shell, feature, operation,
  settings, native, diagnostics, and package needs.
- Permit only bounded temporary duplication needed to observe real commonality.
- Capture the before-extraction build and native journey for both apps.
- Prove each proposed shared field is used by both consumers or is a necessary
  registry/build invariant.
- Remove proposed shared fields that exist only for hypothetical future apps.
- Extract and migrate both consumers within this one package only after the
  evidence matrix is complete.

### 6.4 Minimal internal template manifest

Define one bounded, versioned, repository-internal application manifest whose
exact fields are fixed by activation and two-consumer evidence. It may include
only proven configuration such as:

- internal application ID and display identity;
- static feature/route/navigation/command/settings contribution selection;
- finite backend operation and native-intent allowlists;
- theme/brand asset references from approved repository roots;
- Tauri window and package identity inputs;
- state, single-instance, sidecar, diagnostics, and updater namespaces;
- exact target/package selections;
- build-time composition flags with finite enumerated values.

Require:

- strict schema validation, bounded strings/collections, stable IDs,
  deterministic normalization, explicit required fields, and unknown-field
  rejection;
- no scripts, arbitrary commands, executable paths, dynamic imports, URLs,
  secret values, environment interpolation, native paths, raw capability
  grants, or untyped payloads;
- no public compatibility promise beyond the two accepted repository
  consumers;
- one internal owner and one current version;
- no speculative migration framework. A later schema change would require
  separately accepted evidence, not an implied successor package.

### 6.5 Deterministic generation or composition

- Use one activation-approved deterministic generator, copier, or static
  composition command.
- Pin every tool and consume only validated repository inputs.
- Refuse an existing non-generated destination unless the exact activated
  overwrite policy proves it is the same generated tree.
- Use path-safe relative outputs beneath exact approved roots.
- Reject traversal, absolute paths, reserved names, case-fold collisions,
  Unicode-normalization collisions, duplicate IDs, duplicate package names,
  duplicate bundle identifiers, and duplicate state namespaces.
- Generate no secrets, credentials, local absolute paths, timestamps, random
  IDs, host-dependent values, or network-fetched content.
- Produce byte-stable output for identical inputs and tool versions.
- Make a second identical run idempotent and clean.
- Detect manual edits to generated files and manifest/output drift.
- Keep generated ownership markers machine-readable and bounded without
  weakening source review.

### 6.6 Shared application bootstrap and shell

- Extract only bootstrap/provider/native-background/theme-before-paint,
  portal, responsive shell, title-bar fallback, focus, accessibility, and
  contribution-host behavior proven identical by both apps.
- Preserve accepted P2 theme and shell ownership and the P5 static registry.
- Keep product labels, navigation choices, icons, feature sets, routes, and
  domain states in app-owned definitions.
- Avoid feature-specific branches in shared shell code.
- Preserve keyboard, screen-reader, forced-color, reduced-effect, scaling,
  text expansion, RTL, compact-width, and portal behavior for both apps.
- Do not create a universal component library or `packages/ui`.

### 6.7 Shared static feature and contribution composition

- Reuse the accepted P5 static first-party feature registry and validators.
- Compose only build-time-known trusted features.
- Validate feature, route, path, navigation, command, shortcut, setting, and
  required-operation conflicts per application.
- Keep application feature sets independently selectable through finite
  manifest values.
- Preserve deterministic fail-closed conflict behavior independent of import
  order.
- Keep first-app-only and second-app-only feature behavior private.
- Add no runtime discovery, plugin loading, marketplace, package scanning,
  arbitrary module import, or third-party extension hook.

### 6.8 Contracts, Rust authorization, and Python boundaries

- Keep accepted JSON Schema and generated/derived binding authority shared
  only where both applications consume the same wire contract.
- Give each application an exact finite operation allowlist.
- Keep Rust independently authoritative for operation, native intent, path,
  capability, lifecycle, settings, diagnostics, updater, and process policy.
- Treat frontend manifests as static presentation/composition metadata, never
  native authorization.
- Keep Python dispatch finite and app-aware only through exact authorized
  operation contracts, never a caller-selected module or product script.
- Verify an operation present in one app cannot be invoked by the other unless
  independently allowed.
- Keep sidecar build IDs, schema hashes, supported operation lists, packaged
  resources, and application ownership exact.
- Add no generic invoke, process, shell, path, protocol, environment, or
  payload tunnel.

### 6.9 Identity and state namespace isolation

Each application must have unique, validated values for:

- internal application, feature, route, command, setting, and package IDs;
- Tauri/bundle identifier, executable, installer/package identity, desktop
  integration, and uninstall identity;
- settings document location and schema namespace where separation is
  required;
- cache, temporary, diagnostics, export, native-reference, and log namespaces;
- single-instance lock/registration and launch-intent routing;
- sidecar resource/build identity where application behavior differs;
- updater application identity, channel eligibility, metadata namespace, and
  public verifier policy where activated.

Prove one app cannot read, migrate, overwrite, reset, repair, forward into,
update, uninstall, or become the backend owner for the other app's state.
Shared read-only schemas or assets must not create shared mutable ownership.

### 6.10 Brand and asset isolation

- Keep brand assets and product copy in app-owned, provenance-recorded roots.
- Validate icon/image formats, dimensions, target variants, alpha/color
  requirements, bounded size, and safe decoding.
- Use semantic theme tokens and approved brand accents with contrast evidence.
- Keep legal names, publisher/vendor wording, descriptions, product links, and
  package metadata app-specific.
- Ensure generated packages contain only their own brand assets and copy.
- Detect first-app brand leakage into the second and second-app leakage into
  the first across source, built assets, executable strings where appropriate,
  package inventory, installed UI, diagnostics, and handoffs.
- Do not generate marketing sites, public documentation, or customer content.

### 6.11 Capabilities, CSP, and trust isolation

- Produce or compose one least-privilege production capability set per app
  from finite reviewed inputs; a manifest cannot grant arbitrary permissions.
- Preserve strict production CSP and network denial unless an accepted
  application-specific requirement and targeted amendment say otherwise.
- Prove development/test/fault/evidence surfaces remain excluded from both
  production apps.
- Keep package-signing identity, updater-signing verifier, stable/beta
  eligibility, protected workflows, and staging/publication roles explicit per
  app.
- Share signing or updater trust only when accepted P7 evidence and activation
  explicitly authorize the exact relationship and blast radius.
- Never copy, generate, inspect, log, or package private credentials.
- Do not sign, notarize, publish, or mutate channels merely to extract the
  template; use accepted predecessor trust evidence and only the exact
  activation-authorized verification needed for both package identities.

### 6.12 Build, workspace, and dependency boundaries

- Keep one understandable workspace with explicit shared and app-owned
  packages.
- Extract only dependencies genuinely used by shared code.
- Keep app-only dependencies and build inputs app-owned.
- Preserve locked Rust, JavaScript, Python, Tauri/plugin, generated,
  packaging, and workflow closures.
- Make both application build commands explicit and independently runnable.
- Prevent one app build from silently depending on the other's generated,
  cached, installed, or mutable output.
- Keep target artifacts, intermediate directories, package filenames, and
  evidence roots collision-free.
- Reconcile licenses, notices, SBOM inputs, and package inventories for each
  application without claiming public release.

### 6.13 Fresh-copy and reproducibility proof

For each application independently:

- start from a clean accepted repository checkout;
- materialize into an empty approved destination;
- validate the manifest before output;
- generate or compose exact source without network or host-local inputs;
- rerun generation and prove byte stability/idempotence;
- restore locked dependencies;
- run schema/generation/type/Rust/Python tests as applicable;
- build frontend, sidecar, native host, and exact target package;
- inspect package contents, identity, capabilities, CSP, assets, operations,
  state namespaces, and forbidden files;
- install on the accepted clean-target or equivalent;
- run the app's accepted native journey;
- exercise settings, single-instance, diagnostics, backend, recovery, and
  cleanup relevant to that app;
- uninstall/remove and verify cross-app state remains untouched.

Also create one simultaneous-install journey proving both applications coexist,
launch independently, retain separate state/backends, and clean up without
collision.

### 6.14 Tests for negative and drift behavior

Add proportional tests for:

- invalid/unknown/oversized manifest fields;
- missing required identity or brand inputs;
- path traversal, absolute path, separator, symlink, reserved-name, case-fold,
  and Unicode-normalization collisions;
- duplicate app, bundle, executable, package, feature, command, setting,
  state, instance, sidecar, updater, and channel namespaces;
- generated-output drift and manual-edit detection;
- import-order-independent contribution validation;
- wrong-app feature, operation, capability, state, package, update, and
  diagnostic access;
- brand and product-copy leakage;
- secrets, native paths, environment values, credentials, and user data in
  source, output, packages, logs, screenshots, CI, or deliverables;
- dynamic import, plugin, arbitrary command, network, and public-SDK negative
  checks;
- clean regeneration after failure and no partial destination acceptance.

### 6.15 Evidence-driven extraction and non-extraction record

For every candidate surface, the implementation report must record:

- first-app path and use evidence;
- second-app path and use evidence;
- exact invariant or shared implementation need;
- exact app-owned variants;
- final owner and API/configuration boundary;
- tests and native journeys proving both consumers;
- migration effect on each app;
- omitted or rejected fields;
- why the result remains internal and does not justify a public contract.

Any candidate without two real use sites or one necessary safe-composition
invariant remains private, direct, or removed.

### 6.16 Accessibility, privacy, and native truth

- Preserve accepted accessibility and responsive evidence independently for
  both brands and all claimed targets.
- Keep user content, settings values, diagnostics payloads, paths, raw launch
  input, environment data, credentials, and native-reference backing values
  out of manifests, generated source, logs, screenshots, CI, and handoffs.
- Use only synthetic bounded fixtures.
- Keep source, browser, native source-run, package creation,
  installed/native, engine, accessibility, process, CI, manual, and
  unavailable evidence separate.
- One application, brand, target, engine, or evidence class cannot substitute
  for another.

### 6.17 Focused documentation and handoff

Document only:

- internal template ownership and supported manifest version;
- shared versus app-owned source boundaries;
- exact composition/generation and drift commands;
- how the two accepted applications declare identity, brand, features,
  operations, state namespaces, packages, and trust;
- fresh-copy build/package/install/native verification;
- known limitations, non-extracted surfaces, and protected boundaries;
- explicit absence of public API, plugin, publication, rollout, and successor
  authority.

Do not begin prompt-pack consistency review, rewrite roadmap authority, create
public template documentation, or imply support for an unnamed third app.

## 7. Explicit exclusions and prohibited work

The future package must not:

- activate, repair, reimplement, or rerun predecessors as substitute work;
- begin a prompt-pack consistency review or modify another work-package
  prompt;
- create a third application, sample app, demo gallery, template marketplace,
  broad product family, or successor package;
- create `packages/ui`, a public module SDK, public application SDK, public
  template package, public generator, scaffold CLI, plugin API, runtime plugin
  system, extension marketplace, compatibility promise, or dynamic registry;
- extract a surface supported by only one consumer or a hypothetical future
  need;
- move app-specific domain logic, copy, routes, features, assets, settings,
  operations, package metadata, or trust policy into shared code merely to
  reduce line count;
- add arbitrary scripts, commands, imports, URLs, native paths, capabilities,
  environment interpolation, or untyped payloads to a template manifest;
- weaken Rust authorization, finite operation dispatch, native path secrecy,
  settings single-writer ownership, diagnostics privacy, single-instance
  ownership, updater verification, process containment, no replay, CSP,
  capabilities, or production exclusions;
- share mutable settings, logs, diagnostics, cache, temporary files, native
  references, single-instance locks, updater state, or backend ownership across
  applications;
- copy, create, inspect, rotate, expose, or package private keys, tokens,
  passwords, credential values, notarization secrets, store credentials, or
  publication credentials;
- perform signing, notarization, channel mutation, or external service action
  beyond exact separately activated verification necessary to prove the two
  package identities;
- publish a GitHub Release, public package, public template, documentation
  site, update channel, store submission, customer artifact, or enterprise
  deployment;
- begin public publication, customer rollout, marketing, telemetry, remote
  support, accounts, licensing, monetization, or unrelated product/customer
  work;
- add extra targets, architectures, package formats, distributions, stores, or
  support claims beyond activation;
- redesign accepted architecture, shell, theme, operation/runtime, settings,
  diagnostics, release trust, or repository history;
- perform opportunistic upgrades, broad refactors, formatting sweeps, or
  unrelated cleanup;
- open a PR, enable auto-merge, merge, rebase, squash, force-push, rewrite
  accepted history, delete a branch, or change repository settings.

If an excluded action is required, stop and return `Blocked` with the smallest
specific decision needed. Final-roadmap status does not authorize broader work.

## 8. Allowed and protected paths

Future activation must replace these broad categories with the smallest exact
path allowlist from accepted merged `main`:

```text
workspace manifests and locks                 # explicit multi-app/shared membership only
apps/first-application-or-accepted-path/       # exact first-app migration and app-owned definitions
apps/second-application-or-accepted-path/      # one approved real second branded application
packages/app-template/ or accepted internal root
                                                # bounded internal manifest/composition source
packages/app-contracts/                        # shared schemas only when both apps consume them
packages/app-shell/ or accepted internal root  # only proven two-app bootstrap/shell invariants
packages/app-features/ or accepted roots       # static shared contribution types/registry only
services/python-backend/                       # proven shared finite protocol/handlers only
scripts/generate-app/ or accepted script path  # deterministic bounded composition
scripts/verify/                                # collision, drift, package, privacy, coexistence checks
focused tests and synthetic fixtures           # two-app positive and negative evidence
.github/workflows/                             # minimal two-app build/package evidence only
focused internal template documentation        # ownership, manifest, commands, limitations
```

The literal category names above do not authorize creating those directories.
Activation must remove nonexistent and unnecessary entries, preserve accepted
owners, and name every exact allowed path.

Protected by default:

- repository authority, Phase 0A records, Stage 1 governance, prompt files,
  accepted reports, evidence, and snapshots;
- accepted architecture and predecessor behavior except exact two-consumer
  extraction/migration;
- first-app and second-app domain behavior outside the approved brief;
- credentials, keychains, secret-manager state, external service settings,
  protected-environment configuration values, public release systems, stores,
  and repository settings;
- public API/package/plugin/generator surfaces;
- public publication, rollout, marketing, customer, enterprise, and telemetry
  surfaces.

Every changed path must appear in the implementation report with purpose,
source owner, both-consumer evidence where shared, and activation authority.
Generated caches, dependency stores, build output, packages/installers, signed
artifacts, native captures, local environments, credentials, settings, logs,
user content, and temporary files must not be committed.

## 9. Ordered future implementation sequence

All steps below are future requirements. This provisional prompt performs none
of them.

1. Read all activated authority, predecessor evidence, and the approved
   second-application brief; issue the exact acknowledgement and verify every
   stop condition.
2. Verify repository access, exact accepted merged `main`, fresh branch, clean
   tree, exact allowlist, real consumer ownership, target/evidence ownership,
   absent public/successor work, and activation freshness.
3. Generate exactly one task-start UTC run ID, resolve all four implementation
   deliverable names, and stop on any collision.
4. Inventory both applications and every candidate shared, variant, protected,
   generated, build-only, and app-owned surface before extraction.
5. Freeze exact first/second identity, brand, assets, features, operations,
   state namespaces, capabilities, packages, targets, trust disposition,
   manifest schema, tools, paths, and evidence budgets.
6. Run accepted first-application regressions and capture its exact
   pre-extraction source, build, package, native, identity, state, and trust
   baseline.
7. Implement the smallest approved real second application through existing
   accepted direct/static patterns without creating a template first.
8. Prove the second app's distinct brand, bounded domain journey, static
   features, finite operations, state ownership, accessibility, package
   identity, and real native execution before extraction.
9. Capture a two-application use-site, ownership, invariant, variant, and
   non-extraction matrix for every candidate surface.
10. Define the smallest strict internal template-manifest schema and one
    version using only two-consumer fields and necessary composition
    invariants.
11. Implement deterministic path-safe bounded generation or static
    composition with pinned tools, collision refusal, idempotence, and drift
    detection.
12. Extract only proven bootstrap, shell, contribution, contract, build, and
    verification surfaces; keep domain, brand, identity, state, capability,
    package, and trust variants app-owned.
13. Migrate both applications to the shared internal surfaces and one
    validated build-time composition path.
14. Enforce independent application, bundle, executable, package, settings,
    cache, diagnostics, single-instance, sidecar, updater, channel, and
    uninstall identities.
15. Enforce per-app finite features, operations, native intents,
    capabilities, permissions, CSP, signing/updater disposition, and
    production test-surface exclusion.
16. Add deterministic validation for manifest, asset, path, identifier,
    namespace, contribution, operation, capability, package, generated-output,
    and brand-leakage conflicts.
17. Add negative tests for traversal, normalization, case folding, symlinks,
    manual generated edits, wrong-app state/operation/update access, dynamic
    loading, generic tunnels, secret/path leakage, and partial generation.
18. Materialize each app independently into a fresh empty destination, repeat
    generation, and prove byte stability, idempotence, exact inventory, and
    clean dependency restoration.
19. Build, test, package, inspect, install, and run the exact accepted native
    journey for each app on every claimed target, keeping evidence separate.
20. Run simultaneous-install/coexistence, independent single-instance,
    settings, diagnostics, backend, update-boundary, uninstall, cleanup, and
    cross-state negative journeys.
21. Retain raw generation, build, package, install, launch, feature, settings,
    diagnostics, backend, memory, size, coexistence, uninstall, and cleanup
    measurements and limitations.
22. Reinspect source, generated output, manifests, assets, packages,
    capabilities, CSP, workflows, logs, screenshots, CI, and handoffs for
    product leakage, secrets, paths, unsupported claims, public surfaces, and
    unrelated work.
23. Reconcile the exact diff, extraction matrix, status inventory, target
    claims, documentation, and all non-extraction decisions; remove
    speculative abstractions, duplicate owners, dead scaffolding, and hidden
    stubs; rerun all checks.
24. Commit one coherent package, perform an immediate remote-race check, and
    ordinary fast-forward push only the activated branch; leave `main`
    unchanged and open no PR.
25. Build the source snapshot from the exact final commit, verify one safe
    root, CRC/path/inventory integrity and digest, extract into a fresh empty
    directory, rerun every source-reproducible check, finalize exactly four
    deliverables and all gate/status evidence, return
    `READY FOR CHAT SESSION REVIEW`, and stop.

Do not create a framework before the real second consumer, extract
one-consumer behavior, treat generation as runtime authority, substitute one
app or target for another, begin public publication, or invent successor work.

## 10. Cross-cutting rules and invariants

### Evidence before abstraction

- Two real applications or one necessary composition invariant justify each
  shared surface.
- The second app must run before extraction begins.
- Types, fixtures, mocks, stubs, screenshots, renamed copies, and manifests
  alone are not consumers.
- App-owned domain and brand behavior remains app-owned.
- The extraction matrix records every decision and rejected abstraction.

### Ownership and trust

- Rust remains authority for operations, capabilities, native paths, settings
  writes, diagnostics export, updater verification, sidecar/process lifecycle,
  and single-instance behavior.
- React owns bounded presentation and user intent only.
- Python owns finite authorized computation only.
- Template metadata is trusted static build input, not native authorization.
- Each mutable datum has exactly one writer and belongs to exactly one
  application unless accepted evidence proves read-only sharing.

### Static composition

- Applications and features are statically selected at build time.
- No runtime plugin loading, package scanning, marketplace, dynamic import,
  service locator, dependency injection container, arbitrary hooks, scripts,
  or caller-provided code.
- Identical validated inputs under identical pinned tools yield byte-stable
  output.
- Invalid, conflicting, ambiguous, stale, or manually drifted output fails
  closed.

### Identity and isolation

- Application, bundle, executable, package, settings, cache, diagnostics,
  single-instance, sidecar, updater, channel, install, and uninstall identities
  are explicit and collision-free.
- One app cannot mutate, repair, update, route into, or own the other's state.
- Shared read-only code does not imply shared mutable state or trust.
- Brand, product copy, domain semantics, capabilities, target claims, and
  package metadata remain exact per app.

### Security and privacy

- No manifest field grants arbitrary native/backend authority.
- No generic command, operation, filesystem, process, shell, path, URL,
  environment, capability, protocol, or payload tunnel.
- Private material never enters source, generated output, packages, logs,
  caches, screenshots, CI, diagnostics, SBOM/notices, or deliverables.
- User content, settings values, native paths, raw launch inputs, environment
  data, diagnostic payloads, and opaque-reference backing values remain
  excluded.
- CSP, network denial, safe rendering, production separation, process
  containment, and no replay remain accepted and least privilege.

### Accessibility and native truth

- Both branded applications independently preserve keyboard, focus,
  screen-reader, non-color, forced-color, reduced-effect, 200% scaling, text
  expansion, RTL, compact-shell, portal, title-bar, and native behavior.
- Browser/source/package/native/engine/manual evidence stays separate.
- One app, target, package, engine, runner, or evidence class cannot
  substitute for another.
- Brand distinction must not reduce contrast, clarity, focus visibility, or
  assistive-technology semantics.

### Reproducibility and scope

- Use exact pins, locks, deterministic synthetic fixtures, clean checkouts,
  fresh destinations, and reviewed commands.
- Bound manifest size, asset size, generated file count/bytes, paths,
  identifiers, registries, operations, package inventories, evidence, and
  execution time.
- Record exact commands, tools, runners, OS/architecture, engines, packages,
  commits, artifacts, raw measurements, classifications, and limitations.
- Perform no opportunistic upgrade, broad refactor, formatting sweep, public
  extraction, publication, or unrelated cleanup.

### Status vocabulary

- Implementation status uses only `Implemented`, `Partially implemented`,
  `Stub`, `Mock-only`, `Not started`, or `Blocked`.
- Evidence status uses only `Passed`, `Failed`, `Partial`, `Blocked`, or
  `Not run`.
- `Partially implemented`, `Partial`, and `Blocked` require a concise reason
  and exact evidence reference.
- Prompt acceptance, activation, implementation review, PR authorization,
  merge authorization, public publication, and customer rollout are separate
  decisions.

## 11. Required validation, evidence, and measurements

Future validation must keep these evidence classes separate:

1. authority, activation, exact base, branch, permission, consumer-owner, and
   path-allowlist evidence;
2. accepted predecessor commit, report, artifact, target, trust, limitation,
   and blocker evidence;
3. approved second-application brief, brand, asset provenance, domain,
   feature, operation, target, package, and owner evidence;
4. complete before-extraction first/second source and ownership inventory;
5. two-consumer use-site, invariant, variant, and non-extraction matrix;
6. manifest schema, validation, bounds, normalization, reserved identifiers,
   and unknown-field evidence;
7. deterministic generator/composer tool, path safety, collision, idempotence,
   drift, manual-edit, and partial-failure evidence;
8. shared bootstrap/shell/provider/theme/portal/title-bar/accessibility
   evidence;
9. static feature/route/navigation/command/shortcut/settings contribution and
   conflict evidence;
10. shared schema/generated-binding and per-app finite operation/Rust
    authorization/Python dispatch evidence;
11. app, bundle, executable, package, settings, diagnostics, instance,
    sidecar, updater, channel, install, and uninstall identity evidence;
12. brand asset, product-copy, metadata, contrast, provenance, and cross-brand
    leakage evidence;
13. per-app capabilities, permissions, CSP, production/test separation,
    signing/updater disposition, and secret-negative evidence;
14. locked workspace, dependency, build, sidecar, generated, packaging,
    license, notice, and SBOM-input evidence;
15. first-app fresh materialization, source build, package, installed/native,
    feature, settings, diagnostics, backend, recovery, and cleanup evidence;
16. second-app fresh materialization, source build, package,
    installed/native, feature, settings, diagnostics, backend, recovery, and
    cleanup evidence;
17. simultaneous installation, independent launch/single-instance/state,
    backend/process, update-boundary, uninstall, and cross-app negative
    evidence;
18. accessibility, engine, target, CI, manual, performance, size, reliability,
    unavailable, and limitation evidence;
19. source snapshot, fresh extraction, exact inventory, digest, path safety,
    and complete source-reproducible rerun evidence.

For every command or check, retain exact method, exit status, expected and
actual result, commit, application, target OS/version/architecture, engine,
package, runner/hardware, tool/version, artifact identity, capture time, raw
measurement, classification, and limitation.

Future scenarios must include at minimum:

- second app is a real bounded consumer before extraction;
- every shared field maps to both apps or one necessary invariant;
- manifest invalid, unknown, oversized, malformed, and ambiguous inputs fail;
- paths cannot traverse, escape, collide by case or Unicode normalization,
  follow an unsafe link, or overwrite app-owned source;
- repeated generation is byte-stable and idempotent;
- manual generated drift and partial output fail;
- app, bundle, executable, package, settings, cache, diagnostics,
  single-instance, sidecar, updater, channel, install, and uninstall
  identifiers are collision-free;
- both apps build from clean locked dependencies without consuming each
  other's mutable outputs;
- both apps contain only their own brand assets, product copy, features,
  operations, capabilities, and package identity;
- wrong-app operations, settings, diagnostics, launch intents, update
  metadata, and repairs fail safely;
- template metadata cannot authorize Rust operations or capabilities;
- dynamic loading, generic tunnels, public SDK exports, and production
  test/fault/evidence surfaces are absent;
- secrets, credentials, native paths, user content, settings values,
  environment data, and diagnostic payloads are absent from all outputs;
- each app independently passes source, package, installed/native,
  accessibility, state, backend, diagnostics, recovery, and cleanup journeys;
- both apps coexist without state, lock, backend, update, package, or uninstall
  collisions;
- one app's uninstall or reset does not alter the other's valid state;
- every target and unavailable result remains separate and truthful;
- no public publication, customer rollout, consistency review, PR, merge, or
  successor work occurs;
- the source snapshot extracts safely and reproduces every source-level
  result.

The review evidence index must map every acceptance gate independently to
source/tests, command and exit status, workflow run/job/artifact,
application/target/native/manual proof, measurement, classification,
limitation, accepted exception if any, and deliverable digest.

## 12. Measurable acceptance gates

All 30 gates are unsatisfied while this prompt remains provisional:

1. Exact accepted and merged predecessors through P7-WP02 are used from the
   exact clean activated `main`, with every relevant report, artifact,
   limitation, exception, and blocker reconciled.
2. A real approved second branded application has an accountable owner,
   non-placeholder identity/assets, bounded domain and feature needs, exact
   targets/packages, evidence owners, and explicit exclusions.
3. The final diff remains inside the activated one-package allowlist with no
   consistency-review rewrite, third app, public SDK/plugin, publication,
   customer rollout, successor, prompt-pack, repository-setting, or unrelated
   work.
4. Both applications are implemented and proven through current accepted
   direct/static patterns before shared template extraction begins.
5. A complete before-extraction matrix maps every candidate to both real use
   sites, a necessary invariant, app-owned variants, owner, evidence, and
   explicit extraction or non-extraction decision.
6. Every extracted surface is used by both applications or is required to
   compose them safely; one-consumer, speculative, and domain-specific
   surfaces remain private or are removed.
7. One strict bounded versioned internal template manifest contains only
   evidence-backed static fields, rejects unknown/unsafe inputs, and grants no
   arbitrary executable, native, capability, path, URL, environment, or
   payload authority.
8. One pinned deterministic generation or composition workflow validates
   before writing, emits only path-safe approved outputs, refuses collisions
   and unsafe overwrite, and leaves no accepted partial destination.
9. Identical validated inputs and tools produce byte-stable idempotent output;
   generated drift and manual edits are detected and fail.
10. Path traversal, absolute paths, reserved names, unsafe links, separator,
    case-fold, Unicode-normalization, and destination collisions fail
    deterministically.
11. Both apps use the smallest shared bootstrap, provider, theme, portal,
    title-bar, shell, responsive, focus, and accessibility surfaces without
    app-specific branches in shared hosts.
12. Both apps use the accepted static feature/contribution registry and
    deterministic route, navigation, command, shortcut, setting, and
    required-operation validation with no runtime loading or silent override.
13. Shared contracts and generated bindings remain canonical where genuinely
    shared, while each app has an exact finite operation/native-intent
    allowlist and Rust authorization remains independent of template metadata.
14. Python remains finite typed authorized computation with exact app-aware
    packaged identity and no dynamic module, arbitrary handler, path, process,
    network, or product-script dispatch.
15. Application, bundle, executable, package, settings, cache, diagnostics,
    single-instance, sidecar, updater, channel, install, and uninstall
    identities are explicit, normalized, unique, and collision-free.
16. One app cannot read, write, migrate, reset, repair, forward into, update,
    uninstall, or own the other's settings, diagnostics, references, tasks,
    backend, or mutable state.
17. Each app package contains only its approved brand assets, product copy,
    identity, features, operations, resources, licenses/notices, and metadata;
    cross-brand or domain leakage fails.
18. Brand assets have exact provenance, safe formats/bounds, target variants,
    semantic-token use, contrast, and accessibility evidence.
19. Each app has a least-privilege production capability/permission set and
    strict CSP; template inputs cannot grant arbitrary authority and
    development/test/fault/evidence surfaces are absent from production.
20. Application signing/updater verifier/channel/protected-workflow
    relationships remain exactly activation-approved, private material is
    absent, and no signing/publication authority is inferred from extraction.
21. Locked workspace, Rust, JavaScript, Python, Tauri/plugin, sidecar,
    generated, packaging, workflow, license, notice, and package-component
    closure remains reproducible and app-specific where required.
22. Each app independently materializes into a fresh empty destination,
    restores locked dependencies, passes source/schema/generation/type/Rust/
    Python checks, and builds its exact native package.
23. Each app's package passes exact identity, architecture, capability, CSP,
    brand, operation, state-namespace, resource, permission, and
    forbidden-file inspection.
24. Each app installs and launches natively on every claimed target and passes
    its bounded feature, settings, single-instance, diagnostics, backend,
    recovery, accessibility, and clean-shutdown journey.
25. Both apps coexist when installed, launch independently, keep distinct
    state/backends/update boundaries, and uninstall or reset without altering
    the other's valid data.
26. No public SDK, `packages/ui`, runtime plugin, marketplace, public
    generator, third app, generic tunnel, telemetry, remote support, public
    publication, customer rollout, or unrelated product work enters the
    package.
27. Source, generated, browser, package, installed/native, engine, process,
    accessibility, CI, manual, and unavailable evidence remains separate and
    truthful for both applications and every target.
28. Raw generation, build, package, install, launch, feature, state, backend,
    memory, size, coexistence, reliability, uninstall, and cleanup
    measurements are retained without fabricated support, rollout, or
    performance claims.
29. Exactly four implementation deliverables use one task-start run ID, have
    verified hashes, contain no sensitive data, and represent the reviewed
    final commit; the source snapshot has one safe root, passes
    CRC/path/inventory checks, extracts cleanly, and reproduces all
    source-reproducible validation.
30. The implementation branch matches remote, `main` is unchanged, no PR or
    auto-merge exists, and handoff stops at review without consistency review,
    public publication, customer rollout, merge, branch deletion, or
    successor work.

If a gate is unmet, report `Blocked`, `Not run`, `Partial`, or the exact
accepted limitation as an evidence outcome. Never aggregate independent app,
target, package, namespace, trust, or evidence facts into a vague pass.

## 13. Stop conditions and blocker reporting

Stop before writing or at the last safe non-destructive point when:

- activation is absent, provisional, stale, incomplete, or overbroad;
- repository, exact base, branch, clean tree, allowlist, permission, target,
  package, publication, or evidence-owner state differs;
- P7-WP02 or another predecessor is unaccepted or unmerged;
- the second app lacks a real approved owner, domain need, brand, identity,
  target matrix, package plan, feature/operation need, or evidence owner;
- the proposed second consumer is only a rename, fixture, stub, theme variant,
  sample, mock, or post-hoc justification;
- accepted predecessor reports, source, artifacts, trust, package, target,
  limitation, deviation, exception, or blocker facts are unavailable or
  inconsistent;
- a proposed shared surface lacks two real consumers or one necessary
  composition invariant;
- exact manifest, path, generation, overwrite, collision, idempotence, drift,
  asset, namespace, capability, package, or trust rules cannot be resolved;
- extraction would weaken Rust authority, path secrecy, settings ownership,
  diagnostics privacy, single-instance behavior, updater verification,
  process containment, no replay, CSP, capabilities, or production exclusion;
- both applications cannot have independent identity, state, backend,
  package, update, and uninstall boundaries;
- a public SDK, plugin system, dynamic loading, universal UI library, public
  generator, third app, extra target/format, publication, rollout, or unrelated
  product work is required;
- private material, credentials, native paths, user content, settings values,
  environment data, diagnostic payloads, or sensitive values could enter
  source, generated output, packages, logs, evidence, or deliverables;
- one application, target, package, engine, runner, mock, or evidence class
  would have to substitute for another;
- real fresh-copy generation, native packaging, installed journeys, or
  coexistence cannot be proven where a pass would be claimed;
- source snapshot integrity or fresh extraction rerun fails;
- unauthorized or unrelated files enter the diff;
- remote state changes, push is non-fast-forward, or history rewriting would
  be required.

When blocked:

1. Stop at the last clean non-destructive state.
2. Do not broaden scope, invent a consumer, weaken a gate, extract
   speculatively, publish, or create successor work.
3. Record the exact failed fact, command/path, expected and observed state,
   application/target, affected gates, changed paths, tree state, evidence
   class, and unaffected exclusions.
4. Distinguish repository defect, stale activation, missing predecessor
   evidence, consumer/brand decision, extraction-design gap, identity/
   namespace collision, tool/environment limitation, permission, target/
   runner, package, trust boundary, or publication decision.
5. Use `Blocked` or `Partially implemented` for implementation status and
   `Partial` only for explicitly labeled evidence.
6. Return the smallest safe Chat Session decision, focused correction,
   refreshed activation, consumer clarification, or separate authority needed.

## 14. Required functional and status inventory

Before activation, the truthful inventory is:

```text
Stage 1 foundation: Implemented and accepted
GFD-P0B-WP02 provisional prompt: Implemented and accepted as Approved provisional
GFD-P0B-WP02 activation/implementation: Not started
GFD-P0B-WP03 provisional prompt: Implemented and accepted as Approved provisional
GFD-P0B-WP03 activation/implementation: Not started
GFD-P1-WP01 provisional prompt: Implemented and accepted as Approved provisional
GFD-P1-WP01 activation/implementation: Not started
GFD-P2-WP01 provisional prompt: Implemented and accepted as Approved provisional
GFD-P2-WP01 activation/implementation: Not started
GFD-P2-WP02 provisional prompt: Implemented and accepted as Approved provisional
GFD-P2-WP02 activation/implementation: Not started
GFD-P3-WP01 provisional prompt: Implemented and accepted as Approved provisional
GFD-P3-WP01 activation/implementation: Not started
GFD-P3-WP02 provisional prompt: Implemented and accepted as Approved provisional
GFD-P3-WP02 activation/implementation: Not started
GFD-P4-WP01 provisional prompt: Implemented and accepted as Approved provisional
GFD-P4-WP01 activation/implementation: Not started
GFD-P5-WP01 provisional prompt: Implemented and accepted as Approved provisional
GFD-P5-WP01 activation/implementation: Not started
GFD-P6-WP01 provisional prompt: Implemented and accepted as Approved provisional
GFD-P6-WP01 activation/implementation: Not started
GFD-P6-WP02 provisional prompt: Implemented and accepted as Approved provisional
GFD-P6-WP02 activation/implementation: Not started
GFD-P7-WP01 provisional prompt: Implemented and accepted as Approved provisional
GFD-P7-WP01 activation/implementation: Not started
GFD-P7-WP02 provisional prompt: Implemented and accepted as Approved provisional
GFD-P7-WP02 activation/implementation: Not started
GFD-P8-WP01 package outcome: Not started
Approved real second-application brief: Not started
Second-application brand and asset authority: Not started
Second-application bounded domain journey: Not started
Second-application static feature contributions: Not started
Second-application finite operations/native intents: Not started
Second-application settings/diagnostics integration: Not started
Second-application target/package identities: Not started
Before-extraction two-application inventory: Not started
Two-consumer extraction/non-extraction matrix: Not started
Internal template manifest schema: Not started
Deterministic generator or composition workflow: Not started
Path, identifier, and namespace collision validation: Not started
Generated-output idempotence and drift guard: Not started
Shared bootstrap/provider/theme/shell extraction: Not started
Shared static contribution composition: Not started
Shared contract and generated-binding extraction: Not started
Per-app Rust operation/capability authority: Not started
Per-app Python/sidecar identity: Not started
Per-app settings/cache/diagnostics isolation: Not started
Per-app single-instance/update/uninstall isolation: Not started
Per-app brand/product/package isolation: Not started
Per-app CSP and production exclusion: Not started
First-app migration and regression: Not started
Second-app fresh-copy source/native proof: Not started
First-app fresh-copy source/native proof: Not started
Simultaneous-install and coexistence proof: Not started
Cross-app negative and leakage evidence: Not started
Accessibility and responsive evidence for both apps: Not started
Cross-platform packaged/native evidence: Not started
Performance, size, and reliability measurements: Not started
Focused internal template documentation: Not started
Supporting infrastructure: Not started
Tests: Not started
Generated code: Not started
Fixtures/mocks: Not started
Stubs/placeholders: Not started
Incomplete work: Not started
Blocked work: Blocked — predecessors are not accepted and merged, no activated real second-app consumer exists, and GFD-P8-WP01 has no activated prompt
Prompt-pack consistency review: Not started
Public template/SDK/plugin/generator: Not started
Third application or successor package: Not started
Public publication/customer rollout/store/enterprise deployment: Not started
Telemetry/remote support/unrelated product work: Not started
PR creation: Not started
Merge: Not started
Branch deletion: Not started
```

At future completion, update every line independently using only the approved
implementation-status vocabulary. Evidence outcomes may use only `Passed`,
`Failed`, `Partial`, `Blocked`, or `Not run` when explicitly labeled as
evidence. Keep real functionality, infrastructure, tests, documentation,
generated code, fixtures, stubs, incomplete work, blocked work, application
identity, target evidence, and publication state separate.

## 15. One RUN_ID and collision-resistant external naming

At the start of future implementation, after mandatory authority reads and
preflight but before any repository or deliverable write, generate exactly one
UTC run ID in basic ISO-8601 form:

```text
YYYYMMDDTHHMMSSZ
```

Resolve all four implementation filenames immediately and reuse the one value
unchanged. Never substitute a second run ID, local timestamp, target timestamp,
generator timestamp, random suffix, branch label, application name, or build
time.

Before any write, verify none of the four resolved names exists in the
destination. A collision is a stop condition: report `Blocked` without
generating another ID. The run ID identifies one evidence set, not an
application version, template version, release, channel, brand, or target.

## 16. Required deliverables, hashes, and evidence index

Produce exactly these four future implementation deliverables:

1. `prime-shell-work-gfd-p8-wp01-<RUN_ID>-source-snapshot-r1.zip`
2. `prime-shell-work-gfd-p8-wp01-<RUN_ID>-template-extraction-second-branded-application-report-r1.md`
3. `prime-shell-work-gfd-p8-wp01-<RUN_ID>-review-evidence-index-r1.md`
4. `prime-shell-work-gfd-p8-wp01-<RUN_ID>-handoff-manifest-r1.md`

### Source snapshot

- Build from the exact reviewed final commit with one top-level
  `prime-shell/` root and repository-relative paths.
- Include both application sources, shared internal template/composition
  sources, strict manifests, approved brand assets, schemas, generated
  bindings committed by policy, synthetic fixtures, tests, scripts,
  workflows, manifests, locks, and focused internal documentation required
  for review.
- Exclude `.git`, environments, caches, dependency stores, build output,
  packages/installers, signed artifacts, native captures, settings, logs,
  diagnostics, exports, user content, credentials, private material, local
  generated scratch destinations, and unrelated files.
- Verify SHA-256, ZIP CRC, safe paths, expected root, and exact inventory.
- Extract into a fresh empty directory and rerun every source-reproducible
  activated check for both applications.

### Template-extraction and second-branded-application report

Record:

- activation, model/reasoning, repository, branch, base/final commit, parent,
  tree, exact changed paths, and verified snapshot;
- every accepted predecessor commit, report, artifact, digest, evidence,
  deviation, fallback, limitation, exception, amendment, and blocker;
- approved second-app owner, brief, bounded domain, identity, brand/asset
  provenance, features, operations, settings, diagnostics, targets, packages,
  and exclusions;
- complete before/after two-app source, ownership, identity, state,
  capability, package, and trust inventories;
- every extraction and non-extraction matrix row with both use sites,
  invariant, variants, owner, API/configuration, tests, and rationale;
- exact manifest schema/version, bounds, normalization, path/collision rules,
  composition/generation tool, pins, inputs, outputs, overwrite/idempotence,
  drift, failure cleanup, and reproducibility;
- shared bootstrap/shell/contribution/contract surfaces and app-owned
  domain/brand/identity/state/trust surfaces;
- per-app operation, native intent, Rust authority, Python/sidecar,
  capabilities, CSP, settings, diagnostics, single-instance, updater, channel,
  install, and uninstall isolation;
- fresh-copy source, package, installed/native, coexistence, cross-app
  negative, accessibility, privacy, cleanup, and unavailable evidence;
- raw generation/build/package/install/launch/memory/size/reliability
  measurements, all 30 gate outcomes, and the full independent status
  inventory;
- confirmation that consistency review, public SDK/plugin/generator,
  publication, customer rollout, third app, successor, unrelated product, PR,
  and merge work did not start.

### Review evidence index

- Include one row for every one of the 30 acceptance gates.
- Map exact source/tests, command and exit status, workflow run/job/artifact,
  application/target/package/native/manual proof, measurement,
  classification, limitation, accepted exception if any, and digest.
- Keep authority, consumer/brand, inventory/extraction, manifest/generation,
  shared shell, static contributions, contracts/Rust/Python, identity/state,
  brand assets, capability/CSP/trust, dependencies, fresh-copy first app,
  fresh-copy second app, coexistence, accessibility, package/native, privacy,
  performance, snapshot, manual, and unavailable evidence separate.
- Use `None`, `Not run`, `Blocked`, or `Partial` explicitly rather than blank
  cells.

### Handoff manifest and hashing

- List all four deliverables with exact filename, role, byte length, and
  ordinary SHA-256 where applicable.
- Record run ID, snapshot root, reviewed commit/tree/parent/branch, both app
  identities, manifest/tool versions, target/package identities, source and
  generated inventories, results, full status, and next controlled action.
- Record ordinary SHA-256 for the snapshot, implementation report, and
  evidence index.
- If the manifest records its own digest, designate exactly one self-digest
  value. Copy the finalized manifest bytes and replace only that designated
  value with the literal `<SELF_SHA256>`. Compute SHA-256 over those exact
  canonical bytes and record the lowercase hexadecimal result in the
  designated field.
- Reproduce the digest by repeating only that replacement. Do not change the
  field label, colon, spacing, backticks, line endings, byte-length field,
  another digest, or any other byte.
- Recompute every digest after finalization and verify no deliverable changed.
- Never include private material, credentials, user content, settings values,
  diagnostic payloads, environment dumps, native paths, raw opaque
  references, or sensitive brand source material.

Do not create a fifth deliverable, generated-app bundle, installer bundle,
signed-artifact bundle, brand-source archive, native-capture archive, public
template package, or publication artifact. Generated sources, packages,
installed captures, and target results are implementation evidence referenced
inside the four deliverables, not extra external handoff files.

## 17. Branch, draft-PR, review, merge, and deletion controls

This provisional prompt authorizes no implementation branch, generator run,
second-app creation, external-service mutation, or PR. Future activation must:

- start from the exact accepted merged `main` SHA supplied by Chat Session;
- use one fresh implementation branch named by Chat Session;
- never reuse this documentation branch or a predecessor branch;
- keep changes limited to one coherent `GFD-P8-WP01` implementation;
- commit intentionally and ordinary fast-forward push only after complete
  validation and an immediate remote-race check;
- verify remote commit, parent, tree, paths, hashes, and clean parity;
- leave `main` unchanged;
- stop at `READY FOR CHAT SESSION REVIEW`.

Do not open a draft or ready PR unless a later explicit Chat Session
instruction authorizes that separate action. Do not enable auto-merge.
Prompt acceptance is not activation; implementation acceptance does not
authorize public extraction, publication, customer rollout, PR, or merge; and
final-roadmap completion does not imply a successor package.

Merge, squash, rebase, force-push, history rewriting, branch deletion, public
template/SDK/plugin creation, publication, release creation, production-channel
mutation, store submission, customer rollout, enterprise deployment,
telemetry, marketing, third-app work, successor work, consistency review, and
repository-setting changes require separate explicit authority.

## 18. Completion response and return prompt

The future implementation response must lead with exactly one status:

- `READY FOR CHAT SESSION REVIEW`
- `BLOCKED`
- `NOT STARTED`

For `READY FOR CHAT SESSION REVIEW`, report concisely:

- model `GPT-5.6 Sol` and reasoning `High`;
- repository, starting commit, branch, final commit, parent, and tree;
- exact changed paths and verified source snapshot;
- all four deliverable filenames and digests;
- exact first and second application identities, packages, targets, and
  independently isolated state/trust boundaries;
- exact manifest/generator versions and deterministic reproduction commands;
- every extraction and non-extraction result;
- all 30 acceptance-gate outcomes;
- first-app, second-app, coexistence, manifest/generation, identity/state,
  operation/Rust/Python, capability/CSP/trust, package/native, accessibility,
  privacy, CI/manual, snapshot, and unavailable evidence;
- raw measurements, limitations, accepted exceptions, and full status
  inventory;
- confirmation that `main` is unchanged and no PR, merge, branch deletion,
  consistency review, public SDK/plugin/generator, public publication,
  customer rollout, third app, successor, telemetry, or unrelated product
  work occurred.

For `BLOCKED`, report the exact stop condition, command/evidence, repository
state, changed paths, unaffected scope, application/target, affected gates,
and smallest required Chat Session decision. Do not claim a renamed, mock,
stub, generated-only, or partially verified app as a real second consumer.

End a successful future implementation response with:

```text
Chat Session: Review GFD-P8-WP01 on the exact implementation branch and commit
reported above. Read the template-extraction/second-branded-application report,
review evidence index, handoff manifest, and verified source snapshot. Return
Accepted, Focused correction required, or Blocked. Confirm the real second
branded consumer preceded extraction; every shared surface has two consumers
or one necessary invariant; app-owned domain and brand behavior remains
private; the internal manifest and generator/composition flow are bounded,
path-safe, deterministic, idempotent, and drift-checked; both apps retain
independent identity, state, operations, capabilities, packages, trust, and
uninstall behavior; fresh-copy source, installed-native, coexistence,
accessibility, privacy, target, measurement, status, and artifact evidence is
truthful. Prompt-pack consistency review, public SDK/plugin/generator work,
public publication, customer rollout, third-app or successor work, PR
creation, merge, release, and branch deletion remain unauthorized.
```
